주피터 : 코드와 문서를 동시사용 가능
	시각화 바로 가능
	저장된 데이터 로딩 후, 변수값만 바꾸면 가능
==파이참,,, 주피터랑 같은 거,,
시본 = 시각화 패키지

파이선>>
들여쓰기
형 지정 없음
두리뭉실한 파이선 - 자기가 편한대로 한다. 

print("Action : b =a ")
b=a #=는 링크의 의미, 12를 a와 b가 같이 가리키는 거야, 할당XXX
print("a-value : ", a)
print("b-value : ", b)
print("a-id : ", id(a))
print("b-id: ", id(b))

리스트는 값을 모아놓은 것
튜플은 리스트와 비슷하지만, 값은 변경 못함
딕셔너리 : 사전구조(키와 벨류를 하나의 값, 여러개 값을 정하는거)

a//b =>몫
a**b =>에이의 비승

if =>분할이 핵심
	제 삼지대가 생길수가 없음

음수에 인덱스를 쓸수 있기에 => z[:-2,:-2] 
==>머신러닝때 데이터를 따로 떼와서 처리하기 때문에!!

boolean indexing : 찾아내긴 하는데, 논리연산을 찾아내 

PANDAS
==>numpy에서 array를 차원에 따라 많이 썼다면,,, 판다스에서는 
series = > 1차원 배열
dataFrame = >2차원 배열
series => 인덱스 내맘대로 지정 가능 =>주식시장 빨간날 빼고, 날짜별로 접근하기 위해서
	=>시리즈는 numpy array  형태를 맵핑한 형태이다. 


pandas 중요!! =>데이터 핸들링 잘할정도로
dataframe : 가로줄 = index, reindex 세로줄 = columns, rename(이름바꿀때)
모든 연산이 벡터연산이기 때문에
lambda apply 중요!!
	1. 내가 원하는 일이 어떤 규칙으로 동일한 작용을 하니?
	2. 줄단위 : apply, 공통적으로 무엇을 할거니 : lambda 함수 이용
	=>입력 x에 대해서 무엇을 할지에 대한 것


여러 종류 파일 읽고 쓰기>>
1. csv : 순수하게 값만으로 핸들링 =>용량적어서 좋음
2. excel
3. stata
4. json
5. xml
6. ...etc
=>pandas로 여러 파일을 불러들일 수 있다.

























